# 服务集成节点 | n8n-io/n8n | DeepWiki

本文档记录了NextCloud、FileMaker和Yourls等特定服务的集成节点，展示了构建第三方API集成的模式。这些节点为外部服务提供了预构建的接口，将API复杂性抽象为用户友好的配置选项。

## NextCloud集成模式

NextCloud集成展示了第三方服务集成的标准模式。

### NextCloud集成架构

源码参考:
- [NextCloud.node.ts](https://github.com/xionghao1012/n8n/blob/master/packages/nodes-base/nodes/NextCloud/NextCloud.node.ts#L18-L33)
- [GenericFunctions.ts](https://github.com/xionghao1012/n8n/blob/master/packages/nodes-base/nodes/NextCloud/GenericFunctions.ts#L14-L74)
- [NextCloudApi.credentials.ts](https://github.com/xionghao1012/n8n/blob/master/packages/nodes-base/credentials/NextCloudApi.credentials.ts#L9-L57)
- [NextCloudOAuth2Api.credentials.ts](https://github.com/xionghao1012/n8n/blob/master/packages/nodes-base/credentials/NextCloudOAuth2Api.credentials.ts#L3-L59)

## 节点类实现模式

服务集成节点通过标准结构实现`INodeType`接口。

### 服务集成节点类结构

NextCloud类展示了标准节点实现:

| 组件 | 用途 | 实现位置 |
| --- | --- | --- |
| `description` | 节点元数据和UI定义 | [NextCloud.node.ts](https://github.com/xionghao1012/n8n/blob/master/packages/nodes-base/nodes/NextCloud/NextCloud.node.ts#L19-L859) |
| `execute()` | 主要执行逻辑 | [NextCloud.node.ts](https://github.com/xionghao1012/n8n/blob/master/packages/nodes-base/nodes/NextCloud/NextCloud.node.ts#L861-L1311) |
| `usableAsTool` | AI代理兼容性 | [NextCloud.node.ts](https://github.com/xionghao1012/n8n/blob/master/packages/nodes-base/nodes/NextCloud/NextCloud.node.ts#L30-L30) |

## 认证实现模式

NextCloud展示了服务集成中常见的双重认证方法。

### NextCloud认证流程

```
credentials: [
  {
    name: 'nextCloudApi',
    required: true,
    displayOptions: { show: { authentication: ['accessToken'] } }
  },
  {
    name: 'nextCloudOAuth2Api', 
    required: true,
    displayOptions: { show: { authentication: ['oAuth2'] } }
  }
]
```

### 凭证实现

| 凭证类型 | 文件 | 认证方法 |
| --- | --- | --- |
| 访问令牌 | `NextCloudApi.credentials.ts` | 基本认证 |
| OAuth2 | `NextCloudOAuth2Api.credentials.ts` | Authorization头中的Bearer令牌 |

## 通用函数模式

`GenericFunctions.ts`文件实现了标准的API通信模式。

### NextCloud通用函数架构

```
async function nextCloudApiRequest(
  this: IHookFunctions | IExecuteFunctions,
  method: IHttpRequestMethods,
  endpoint: string,
  body: object | string | Buffer,
  headers?: IDataObject,
  encoding?: null | undefined,
  query?: IDataObject
): Promise<any>
```

### FileMaker令牌管理模式

```
async function getToken(this: ILoadOptionsFunctions | IExecuteFunctions): Promise<any> {
  const credentials = await this.getCredentials('fileMaker');
  const requestOptions: IRequestOptions = {
    uri: `https://${host}/fmi/data/v1/databases/${db}/sessions`,
    method: 'POST',
    auth: { user: login, pass: password },
    body: { fmDataSource: [{ database: host, username: login, password }] }
  };
  
  const response = await this.helpers.request(requestOptions);
  return response.response.token;
}
```

## 资源和操作模式

NextCloud展示了标准的资源/操作模式。

### NextCloud资源

| 资源 | 描述 | 关键操作 |
| --- | --- | --- |
| `file` | 文件操作 | 复制、删除、下载、移动、共享、上传 |
| `folder` | 文件夹操作 | 复制、创建、删除、列表、移动、共享 |
| `user` | 用户管理 | 创建、删除、获取、获取全部、更新 |

### 动态参数显示

```
{
  displayName: '文件路径',
  name: 'path',
  type: 'string',
  required: true,
  displayOptions: {
    show: {
      operation: ['upload'],
      resource: ['file']
    }
  }
}
```

## 服务集成执行模式

### NextCloud执行模式

```
for (let i = 0; i < items.length; i++) {
  if (resource === 'file') {
    if (operation === 'download') {
      requestMethod = 'GET';
      endpoint = this.getNodeParameter('path', i) as string;
    } else if (operation === 'upload') {
      requestMethod = 'PUT';
      endpoint = this.getNodeParameter('path', i) as string;
    }
  } else if (resource === 'folder') {
    // 文件夹操作
  } else if (resource === 'user') {
    // 用户管理操作
  }
}
```

### FileMaker令牌管理模式

```
let token;
try {
  token = await getToken.call(this);
} catch (error) {
  throw new NodeOperationError(this.getNode(), error as string);
}

// 使用令牌执行操作
// ...

// 总是登出以清理会话
await logout.call(this, token as string);
```

### Yourls简单模式

```
if (resource === 'url') {
  if (operation === 'shorten') {
    qs.url = url;
    qs.action = 'shorturl';
  } else if (operation === 'expand') {
    qs.shorturl = shortUrl;
    qs.action = 'expand';
  } else if (operation === 'stats') {
    qs.shorturl = shortUrl;
    qs.action = 'url-stats';
  }
}
```

## 高级功能和模式

### NextCloud二进制数据处理

```
if (this.getNodeParameter('binaryDataUpload', i) === true) {
  const binaryPropertyName = this.getNodeParameter('binaryPropertyName', i);
  this.helpers.assertBinaryData(i, binaryPropertyName);
  body = await this.helpers.getBinaryDataBuffer(i, binaryPropertyName);
} else {
  body = this.getNodeParameter('fileContent', i) as string;
}
```

### 错误处理模式

```
if (typeof response === 'string' && response.includes('<b>Fatal error</b>')) {
  throw new NodeOperationError(
    this.getNode(),
    "NextCloud返回了'Fatal error'，请检查描述获取更多详情",
    { description: `服务器响应:\n${response}` }
```